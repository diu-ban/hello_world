#include "main.h"

void print_square(int size)
{
	int row = 0;
	int col;

	if (size > 0)
	{
		while (row < size)
		{
			col = 0;

			while (col < size)
			{
				_putchar('#');
				col++;
			}
			_putchar('\n');
			row++;
		}
	}
	else
	{
		_putchar('\n');
	}
}
