#include<stdio.h>

int main(void)
{
	int sum = 0;
	int ptr = 1;

	while (ptr < 1024)
	{
		if ((ptr % 3 == 0) || (ptr % 5 == 0))
		{
			sum += ptr;
		}
		ptr++;
	}
	
	printf("%d\n", sum);

	return (0);
}
