#include "main.h"

void reverse_array(int *a , int n)
{
	while (n > 1)
	{
		int i = 0;
		int arr[];

		while (i < n)
		{
			arr[i] = a[i];
			i++;
		}
		i--;
		while (i >= 0)
		{
			a[n - i - 1] = arr[i];
			i--;
		}
		
	}
}
