#include<stdio.h>

int main(void)
{
	int i;
	int y;

	for (i = 0; i < 9; i++)
	{
		for (y = i + 1; y <= 9; y++)
		{
			putchar(i + '0');
			putchar(y + '0');

			if (i < 8)
			{
				putchar(',');
				putchar(' ');
			}
		}
	}

	putchar('\n');

	return (0);
}
