#include "main.h"

unsigned int flip_bits(unsigned long int n, unsigned long int m)
{
	unsigned int bits = 0;
	unsigned long int lower = n < m ? n : m;
	unsigned long int higher = n > m ? n : m;

	if (n == m)
		return (0);
	while (higher >> 1)
	{
	  if (
	}
}
